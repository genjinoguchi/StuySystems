Aim: # (can I only use 140 characters for this lesson?)

    
#

	Used to proved preprocessor instructions.

	These driectives are handled before the compiler really kicks in.
	
	#include <LIBRARY> | "LIBRARY"
		 Link libraries to your code

	#define <NAME> <VALUE>
		Replace all occurances of NAME with VAlUE

		#define TRUE 1
		#define FALSE 0
		SUPER USEFUL

		
		macros: (pure search adn replace)
			#define SQUARE(x) x*x
			...
			int y = SQUARE(9); ==> int y = 9 * 9


	Conditional Statements
	
		#ifndef <IDENTIFIER> //IF NOT DEFINED
		<CODE>
		#endif
